# INTENTIONALLY VULNERABLE DEPENDENCIES
#
# These versions are chosen because they have known CVEs in the OSV database.
# DO NOT use these versions in production.

django==4.1.0
requests==2.25.0
flask==2.2.0
celery==5.2.0
jinja2==3.1.1
pillow==9.0.0
pydantic==1.10.0

# App dependencies (current/safe)
fastapi>=0.109.0
uvicorn>=0.27.0
sentry-sdk[fastapi]>=1.40.0
